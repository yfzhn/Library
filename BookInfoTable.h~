/*
*book BookInfoTable
*/
typedef struct BookInfoTable
{
	int index; //索引
	int count; //书总数
	int currentCount;//当前可借图书数	
	struct BookInfoTable *pre; //上一个
	struct BookInfoTable *next; //下一个
}BookTable;
